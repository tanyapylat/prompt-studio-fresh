# Copy this file to .env and fill in a real key to enable live generation.
# Without this, the app runs in fully offline/simulated mode automatically —
# no setup required to click through the workflow.
OPENAI_API_KEY=
