# Agent instructions

This repository is a click-through AI Studio prototype (`app/`) plus product docs.

Before changing the application, its API, CORS, public URLs, or how it is deployed to Azure, read the **Azure Deployment** section in `app/README.md`. Preserve the CORS and public-base-URL constraints documented there, and update that section when implementation or deployment changes.
